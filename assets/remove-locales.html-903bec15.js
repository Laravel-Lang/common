import{_ as e,p as a,q as o,a1 as t}from"./framework-e03faf0e.js";const s={},c=t(`<h1 id="remove-locales" tabindex="-1"><a class="header-anchor" href="#remove-locales" aria-hidden="true">#</a> Remove locales</h1><div class="custom-container warning"><p class="custom-container-title">ATTENTION</p><p>When this command is executed, the entire locale folder with all files is deleted except default and fallback locales.</p></div><p>To delete localizations, you must use <code>lang:rm</code> command, passing the letter abbreviations into it:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>php artisan lang:rm de ro zh-CN
</code></pre></div><p>If you do not specify arguments when passing parameters, then an interactive question will be displayed in the console with a choice of localizations from among the available ones.</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>php artisan lang:rm
</code></pre></div><p>By default, the command will return an error message when trying to remove a protected locale.</p><p>Protected locales are the set codes in the <code>locale</code> and <code>fallback_locale</code> parameters of the <code>config/app.php</code> file.</p><p>To force the deletion of a protected localization, use the <code>force</code> option:</p><div class="language-bash" data-ext="sh"><pre class="language-bash"><code>php artisan lang:rm en <span class="token parameter variable">--force</span>
</code></pre></div>`,10),l=[c];function n(r,i){return a(),o("div",null,l)}const p=e(s,[["render",n],["__file","remove-locales.html.vue"]]);export{p as default};
